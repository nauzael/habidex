import { AdminModule } from './admin.module';

describe('AdminModule', () => {
  it('should be defined with metadata', () => {
    const module = AdminModule;
    expect(module).toBeDefined();
  });
});
